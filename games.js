//Add words array storage

// Add click EventListener to Start button; to call Start game function

// Add click EventListener to Reset button; to call reset game function

// Add keydown EventListener to keydown document; 


// Start game

    //start the timer -> startTimer
    //show the blanks -> show blanks

// Start timer

    //use setInterval to call the function
        //check if time is done
            // call "game over" function
        // else 
            // check if the word is complete
                // call "game won"
            // or else deduct the time count

// show blanks (game round)
    
